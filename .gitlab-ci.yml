image: maven:3.9-eclipse-temurin-17

stages:
  - docker
  - build
  - test

docker-compose:
  stage: docker
  script:
    - docker-compose up -d

build:
  stage: build
  script:
    - mvn clean compile

test:
  stage: test
  script:
    - mvn test
  allow_failure: false
